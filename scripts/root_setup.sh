# Carthage install
command -v carthage >/dev/null || { brew install carthage; }
# SwiftFormat install
command -v swiftformat >/dev/null || { brew install swiftformat; }
# SwiftLint install
command -v swiftlint >/dev/null || { brew install swiftlint; }

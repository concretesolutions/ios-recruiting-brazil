# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Movs' do
  use_frameworks!
 
  pod 'SwiftLint'
  pod 'SnapKit', '~> 5.0.0'

  target 'MovsTests' do
    inherit! :search_paths

    pod 'Nimble', '~> 6.0.0'
    pod 'Quick'

  end

end

import UIKit

public func setupAppearance() {
  UINavigationBar.appearance().tintColor = Asset.Colors.accent.color
  UITabBar.appearance().tintColor = Asset.Colors.accent.color
  UIButton.appearance().tintColor = Asset.Colors.accent.color
}

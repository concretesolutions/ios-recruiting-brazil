disabled_rules:
  - line_length
  - type_name
  - nesting
  - identifier_name
  - trailing_whitespace

excluded: # paths to ignore during linting. Takes precedence over `included`.
  - Carthage
  - Pods